# TEMPORARY diagnostic. Delete once CI is green.
# Variable under test: whether the Actions allowlist permits astral-sh/setup-uv
name: diag-e-uv

on:
  push:
    branches: ["agent/**"]
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: astral-sh/setup-uv@v5
