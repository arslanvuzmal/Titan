[project]
name = "titan-contracts"
version = "0.1.0"
description = "Core data contracts for TITAN"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
